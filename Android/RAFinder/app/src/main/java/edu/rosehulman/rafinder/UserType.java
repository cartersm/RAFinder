package edu.rosehulman.rafinder;

/**
 * Defines the different types of authenticated users that can exist in the system.
 */
public enum UserType {
    NONE,
    RESIDENT,
    RESIDENT_ASSISTANT,
    SOPHOMORE_ADVISOR,
    GRADUATE_ASSISTANT,
    ADMINISTRATOR
}